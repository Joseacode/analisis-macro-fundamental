# IMPORTANTE: SEC pide User-Agent identificable (app + contacto/email)
SEC_USER_AGENT="Institutional Macro Dashboard (Jose) jose@email.com"

# Puerto backend
PORT=8787

# Para no pasar rate limit SEC (ms entre requests). 150~200 es seguro.
SEC_MIN_SPACING_MS=150
